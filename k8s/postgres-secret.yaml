# ⚠️ THIS FILE IS NOT USED ANYMORE
# Secrets are now managed by External Secrets Operator
# See: externalsecret-postgres.yaml
#
# This file is kept as a template/reference only

apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: evite
type: Opaque
stringData:
  POSTGRES_USER: evite
  POSTGRES_PASSWORD: evite  # Change this in production!
  POSTGRES_DB: evite

